RestaurantContext.tsx
│
├── RestaurantContextType
│      → définit ce que le Context contient
│
├── RestaurantContext
│      → crée le Context
│
├── RestaurantProvider
│      → possède le state et fournit les données
│
└── useRestaurantContext
       → permet aux composants de récupérer les données